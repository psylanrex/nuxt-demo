{
  "name": "nuxt-demo",
  "version": "1.0.0",
  "description": "First nuxt app",
  "author": "Kookie",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "nuxt": "^1.0.0"
  },
  "devDependencies": {
    "cross-env": "^5.0.1"
  },
  "config": {
    "nuxt": {
     "port": 8080,
     "host": "0.0.0.0"
    }
  }
}
